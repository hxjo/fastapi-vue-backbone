{
    "dockerComposeFile": "../../docker-compose.yml",
    "service": "fastapi",
    "workspaceFolder": "/srv",
    "shutdownAction": "stopCompose",
    "initializeCommand": "find ~/.ssh/ -type f -exec grep -l 'PRIVATE' {} \\; | xargs ssh-add",
    "postCreateCommand": "poe cmd setup setup-after-docker",
}